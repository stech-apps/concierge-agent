import { QmHighlightPipe } from './qm-highlight.pipe';

describe('QmHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new QmHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
