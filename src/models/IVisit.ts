export interface Visit {
    ticketId: string,
    visitId: number,
    waitingTime: number,
    totalWaitingTime: number,
    appointmentId: number,
    appointmentTime: number,
    queueId: number
  }
